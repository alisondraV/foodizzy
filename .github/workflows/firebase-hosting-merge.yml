# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          echo 'secrects.VUE:' ${{ secrects.VUE }}
          echo 'secrects.VUE_APP_API_KEY:' ${{ secrects.VUE_APP_API_KEY }}
          npm i && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FOODIZZY_APP }}'
          channelId: live
          projectId: foodizzy-app
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          VUE_APP_API_KEY: ${{ secrects.VUE_APP_API_KEY }}
          VUE_APP_AUTH_DOMAIN: ${{ secrects.VUE_APP_AUTH_DOMAIN }}
          VUE_APP_PROJECT_ID: ${{ secrects.VUE_APP_PROJECT_ID }}
          VUE_APP_STORAGE_BUCKET: ${{ secrects.VUE_APP_STORAGE_BUCKET }}
          VUE_APP_MESSAGING_SENDER_ID: ${{ secrects.VUE_APP_MESSAGING_SENDER_ID }}
          VUE_APP_APP_ID: ${{ secrects.VUE_APP_APP_ID }}
          VUE_APP_MEASUREMENT_ID: ${{ secrects.VUE_APP_MEASUREMENT_ID }}
